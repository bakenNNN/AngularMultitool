import { GyerekvedelemModel } from './gyerekvedelem-model.model';

describe('GyerekvedelemModel', () => {
  it('should create an instance', () => {
    expect(new GyerekvedelemModel()).toBeTruthy();
  });
});
